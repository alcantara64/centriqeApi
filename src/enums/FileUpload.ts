export enum FILE_PROCESS_TYPE {
    CUSTOMER_LOAD='customerLoad',
    PRODUCT_LOAD='productLoad',
  }
export enum FILE_UPLOAD_STATUS{
   PENDING = 'pending',
   FAILED = 'failed',
   IN_PROGRESS = 'in-progress',
}
  
  